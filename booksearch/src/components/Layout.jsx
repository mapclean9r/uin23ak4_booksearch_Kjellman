export default function Layout({children}){
    return(
        <>

            <h1>Searchbar</h1>

        <main>
            {children}
        </main>
        <footer>API openlibrary</footer>
        </>
    )
}